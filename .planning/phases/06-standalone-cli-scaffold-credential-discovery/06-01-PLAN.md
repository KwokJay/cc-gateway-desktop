---
phase: 06-standalone-cli-scaffold-credential-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - standalone-cli/package.json
  - standalone-cli/tsconfig.json
  - standalone-cli/README.md
  - standalone-cli/src/index.ts
  - standalone-cli/src/cli.ts
  - standalone-cli/src/output.ts
  - standalone-cli/src/credential-discovery/types.ts
  - standalone-cli/tests/cli-help.test.ts
autonomous: true
requirements:
  - ISO-02
must_haves:
  truths:
    - Operators can see an isolated standalone CLI package with its own manifest, entrypoint, and package-local README.
    - The new CLI help and README state that the standalone surface is additive and does not replace the legacy TypeScript gateway, Rust daemon, Rust CLI, or desktop app.
    - The scaffold compiles and exposes a package-local verification surface without importing protected legacy runtime modules or stale `dist/standalone-cli/` artifacts.
  artifacts:
    - standalone-cli/package.json
    - standalone-cli/tsconfig.json
    - standalone-cli/README.md
    - standalone-cli/src/index.ts
    - standalone-cli/src/cli.ts
    - standalone-cli/src/output.ts
    - standalone-cli/src/credential-discovery/types.ts
    - standalone-cli/tests/cli-help.test.ts
  key_links:
    - `standalone-cli/src/index.ts` routes into `standalone-cli/src/cli.ts` rather than any file under `src/`, `scripts/`, or `crates/`.
    - `standalone-cli/README.md` and package help output both repeat the additive-boundary message required by ISO-02.
    - `standalone-cli/src/credential-discovery/types.ts` defines the typed discovery contracts that Phase 06 Plan 02 will implement against.
---

<objective>
Create the isolated `standalone-cli/` package scaffold and additive operator-facing docs/help surface required for ISO-02 without touching protected legacy paths.

Purpose: Make the new CLI visibly separate from the legacy TypeScript gateway and Rust products before any credential-discovery behavior is added.
Output: `standalone-cli/` package manifest, build config, entrypoint/help modules, typed discovery contracts, and package-local docs/test scaffolding.
</objective>

<execution_context>
@/Users/kay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-RESEARCH.md
@.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-VALIDATION.md
@.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md
@.planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md
@AGENTS.md
@package.json
@tsconfig.json
@dist/standalone-cli/index.js
@dist/standalone-cli/runtime.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the isolated standalone-cli package scaffold and shared contracts</name>
  <files>standalone-cli/package.json, standalone-cli/tsconfig.json, standalone-cli/src/index.ts, standalone-cli/src/cli.ts, standalone-cli/src/output.ts, standalone-cli/src/credential-discovery/types.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-RESEARCH.md
.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md
.planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md
AGENTS.md
package.json
tsconfig.json
dist/standalone-cli/index.js
dist/standalone-cli/runtime.js
  </read_first>
  <acceptance_criteria>
- `standalone-cli/package.json`, `standalone-cli/tsconfig.json`, `standalone-cli/src/index.ts`, `standalone-cli/src/cli.ts`, `standalone-cli/src/output.ts`, and `standalone-cli/src/credential-discovery/types.ts` all exist.
- `standalone-cli/package.json` declares an isolated package name, a `build` script using `tsc -p tsconfig.json`, and a package-local help test script.
- `standalone-cli/tsconfig.json` compiles only the package-local `src/` tree into a package-local `dist/` output.
- `standalone-cli/src/index.ts` and `standalone-cli/src/cli.ts` compile without importing from `src/`, `scripts/`, `crates/`, or `dist/standalone-cli/`.
- `standalone-cli/src/credential-discovery/types.ts` exports the typed discovery result and source identifiers that Plan 02 will implement against.
  </acceptance_criteria>
  <action>Create a new top-level `standalone-cli/` package with `package.json`, `tsconfig.json`, `src/index.ts`, `src/cli.ts`, `src/output.ts`, and `src/credential-discovery/types.ts`. In `package.json`, define a package-local bin name, `build`, `test:help`, and `test:credentials` scripts using the repo’s existing `typescript` and `tsx` toolchain instead of adding dependencies. In `tsconfig.json`, set a package-local `rootDir` and `outDir` so the scaffold stays isolated from the repo-root `tsconfig.json` and `dist/`. Make `src/index.ts` a thin Node entrypoint that delegates to `src/cli.ts`, keep `src/output.ts` responsible for rendered help text, and use `src/credential-discovery/types.ts` to define `DiscoverySource`, `DiscoverySuccess`, `DiscoveryFailure`, and `DiscoveryResult` contracts for later tasks. Treat `dist/standalone-cli/` as a stale artifact risk only: do not copy its command surface and do not import `../oauth.js`, `../proxy.js`, or any protected-path runtime module.</action>
  <verify>
    <automated>test -f standalone-cli/package.json &amp;&amp; test -f standalone-cli/tsconfig.json &amp;&amp; test -f standalone-cli/src/index.ts &amp;&amp; test -f standalone-cli/src/cli.ts &amp;&amp; test -f standalone-cli/src/output.ts &amp;&amp; test -f standalone-cli/src/credential-discovery/types.ts &amp;&amp; rg -n '"name": "ccgw-standalone-cli"|"build": "tsc -p tsconfig.json"|"test:help": "tsx tests/cli-help.test.ts"|"test:credentials": "tsx tests/credential-discovery.test.ts"' standalone-cli/package.json &amp;&amp; rg -n 'DiscoverySource|DiscoverySuccess|DiscoveryFailure|DiscoveryResult' standalone-cli/src/credential-discovery/types.ts &amp;&amp; ! rg -n '\.\./oauth\.js|\.\./proxy\.js|dist/standalone-cli|from .*\.\./src/|from .*\.\./scripts/|from .*\.\./crates/' standalone-cli/src &amp;&amp; npm --prefix standalone-cli run build</automated>
  </verify>
  <done>The isolated package scaffold exists, builds successfully, and defines the shared contracts for later credential-discovery work without crossing the protected-path boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add additive operator docs and package-local help verification</name>
  <files>standalone-cli/README.md, standalone-cli/tests/cli-help.test.ts, standalone-cli/src/cli.ts, standalone-cli/src/output.ts</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-RESEARCH.md
.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-VALIDATION.md
.planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md
AGENTS.md
standalone-cli/package.json
standalone-cli/src/index.ts
standalone-cli/src/cli.ts
standalone-cli/src/output.ts
  </read_first>
  <acceptance_criteria>
- `standalone-cli/README.md` states that the package is additive and does not replace `src/`, `scripts/`, `crates/core/`, `crates/daemon/`, `crates/cli/`, or `crates/desktop/`.
- `standalone-cli/tests/cli-help.test.ts` exists and verifies the rendered help text rather than relying on manual inspection.
- Help output and README both mention the package-local build/test commands and the Phase 06 scope of scaffold plus credential discovery only.
- The automated help test runs without requiring live Claude credentials or a local Keychain item.
  </acceptance_criteria>
  <action>Write `standalone-cli/README.md` so it explains the package purpose, package-local commands (`npm --prefix standalone-cli run build`, `npx tsx standalone-cli/tests/cli-help.test.ts`, and the later credential-discovery test command), and the additive isolation boundary per ISO-02. Update `standalone-cli/src/output.ts` and `standalone-cli/src/cli.ts` so `--help`, `-h`, `help`, and the no-args path all render consistent package-local help text that names this Phase 6 scope as scaffold plus credential discovery only. Add `standalone-cli/tests/cli-help.test.ts` as a direct `tsx` test using Node `assert` to verify the additive messaging, package-local command references, and absence of any legacy replacement wording. Keep the docs/help additive; do not describe proxy startup, config generation, runtime preparation, or `claude` launch behavior from later phases.</action>
  <verify>
    <automated>test -f standalone-cli/README.md &amp;&amp; test -f standalone-cli/tests/cli-help.test.ts &amp;&amp; rg -n 'additive|does not replace|src/|scripts/|crates/core/|crates/daemon/|crates/cli/|crates/desktop/' standalone-cli/README.md &amp;&amp; rg -n 'Phase 6|credential discovery|npm --prefix standalone-cli run build|npx tsx standalone-cli/tests/cli-help.test.ts' standalone-cli/README.md standalone-cli/src/output.ts standalone-cli/tests/cli-help.test.ts &amp;&amp; npx tsx standalone-cli/tests/cli-help.test.ts</automated>
  </verify>
  <done>The standalone package has a checked-in operator-facing boundary explanation and an automated help-surface test that proves the new CLI is additive and isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> standalone CLI docs/help | Misleading help text could make operators think the new package replaces legacy TS or Rust flows. |
| standalone-cli scaffold -> protected legacy paths | Imports or copied command surfaces from legacy modules would violate the Phase 05 isolation boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | S | `standalone-cli/README.md` and help text | mitigate | State explicitly that the package is additive and does not replace `src/`, `scripts/`, `crates/core/`, `crates/daemon/`, `crates/cli/`, or `crates/desktop/`. |
| T-06-01-02 | T | `standalone-cli/src/*` scaffold | mitigate | Forbid imports from protected legacy paths and stale `dist/standalone-cli/` artifacts; verify with `rg` plus package-local build. |
| T-06-01-03 | I | operator-facing output | mitigate | Keep help and README focused on commands and boundaries only; do not print or document credential values, token samples, or runtime secrets. |
| T-06-01-04 | E | package command surface | mitigate | Limit Phase 06 Plan 01 to help/doc scaffolding only and explicitly exclude proxy startup, config generation, runtime preparation, and `claude` launch behaviors from later phases. |
</threat_model>

<verification>
- `npm --prefix standalone-cli run build`
- `npx tsx standalone-cli/tests/cli-help.test.ts`
- `! rg -n '\.\./oauth\.js|\.\./proxy\.js|dist/standalone-cli|from .*\.\./src/|from .*\.\./scripts/|from .*\.\./crates/' standalone-cli/src`
</verification>

<success_criteria>
- The repository contains a new top-level `standalone-cli/` package with its own manifest, entrypoint, and TypeScript build config.
- Package-local help and README clearly describe the surface as additive and isolated from the legacy TS and Rust products.
- The scaffold builds and exposes typed discovery contracts that later Phase 06 work can implement without revisiting package structure.
</success_criteria>

<output>
After completion, create `.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-01-SUMMARY.md`
</output>
