---
phase: 06-standalone-cli-scaffold-credential-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - standalone-cli/src/cli.ts
  - standalone-cli/src/output.ts
  - standalone-cli/src/credential-discovery/discover.ts
  - standalone-cli/src/credential-discovery/parse.ts
  - standalone-cli/src/credential-discovery/sources/keychain.ts
  - standalone-cli/src/credential-discovery/sources/credentials-file.ts
  - standalone-cli/tests/credential-discovery.test.ts
  - standalone-cli/tests/fixtures/credential-discovery/keychain-valid.json
  - standalone-cli/tests/fixtures/credential-discovery/file-valid.json
autonomous: true
requirements:
  - ENV-01
must_haves:
  truths:
    - Operators can run the isolated standalone CLI and have it check supported Claude credential sources in deterministic order.
    - On macOS, credential discovery checks Keychain first via `security find-generic-password -a "$USER" -s "Claude Code-credentials" -w`, then falls back to `~/.claude/.credentials.json`.
    - When no usable credentials are found, or when the credential payload is malformed, the CLI exits with actionable guidance without requiring live local Claude credentials for automated verification.
  artifacts:
    - standalone-cli/src/cli.ts
    - standalone-cli/src/output.ts
    - standalone-cli/src/credential-discovery/discover.ts
    - standalone-cli/src/credential-discovery/parse.ts
    - standalone-cli/src/credential-discovery/sources/keychain.ts
    - standalone-cli/src/credential-discovery/sources/credentials-file.ts
    - standalone-cli/tests/credential-discovery.test.ts
    - standalone-cli/tests/fixtures/credential-discovery/keychain-valid.json
    - standalone-cli/tests/fixtures/credential-discovery/file-valid.json
  key_links:
    - `standalone-cli/src/cli.ts` exposes a credential-discovery command that delegates to `standalone-cli/src/credential-discovery/discover.ts`.
    - `standalone-cli/src/credential-discovery/discover.ts` uses the Plan 01 contracts from `standalone-cli/src/credential-discovery/types.ts` and preserves the source order required by the legacy scripts.
    - `standalone-cli/tests/credential-discovery.test.ts` proves source order, typed parse failures, and actionable error text using fixtures plus inline malformed payload cases and source adapters rather than live Keychain or real `$HOME` credentials.
---

<objective>
Implement deterministic credential-source discovery, typed credential parsing, and fixture-driven verification for ENV-01 inside the isolated `standalone-cli/` package.

Purpose: Let operators verify that the new CLI can safely find supported local Claude credentials without coupling the Phase 6 package to legacy runtime code or live secrets.
Output: Typed discovery modules, minimal CLI wiring for credential discovery, and fixture-backed tests.
</objective>

<execution_context>
@/Users/kay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-RESEARCH.md
@.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-VALIDATION.md
@.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md
@.planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md
@scripts/quick-setup.sh
@scripts/admin-setup.sh
@scripts/extract-token.sh
@standalone-cli/package.json
@standalone-cli/src/cli.ts
@standalone-cli/src/output.ts
@standalone-cli/src/credential-discovery/types.ts

<interfaces>
From `scripts/quick-setup.sh`, `scripts/admin-setup.sh`, and `scripts/extract-token.sh`, preserve this exact discovery order and credential shape:

```text
1. On macOS, run: security find-generic-password -a "$USER" -s "Claude Code-credentials" -w
2. If Keychain yields no credentials, read: ~/.claude/.credentials.json
3. Parse JSON under claudeAiOauth.{accessToken,refreshToken,expiresAt}
4. If nothing usable is found, tell the operator to run `claude` and complete browser login before retrying
```

From `standalone-cli/src/credential-discovery/types.ts`, implement against the Plan 01 contracts instead of inventing new shapes during execution.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write fixture-driven credential-discovery tests and source fixtures</name>
  <files>standalone-cli/tests/credential-discovery.test.ts, standalone-cli/tests/fixtures/credential-discovery/keychain-valid.json, standalone-cli/tests/fixtures/credential-discovery/file-valid.json</files>
  <read_first>
.planning/REQUIREMENTS.md
.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-RESEARCH.md
.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-VALIDATION.md
.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md
scripts/quick-setup.sh
scripts/admin-setup.sh
scripts/extract-token.sh
standalone-cli/src/credential-discovery/types.ts
standalone-cli/src/cli.ts
standalone-cli/src/output.ts
  </read_first>
  <behavior>
    - Test 1: On `darwin`, discovery tries the Keychain source before the credentials file and stops at the first usable credential payload.
    - Test 2: When Keychain is unavailable or empty, discovery falls back to the file source and returns the typed success shape from fixture data.
    - Test 3: Malformed JSON and missing `claudeAiOauth.refreshToken` produce distinct actionable failures instead of being treated as “not found”.
    - Test 4: When both sources miss, the rendered failure tells the operator to run `claude` and complete browser login before retrying.
    - Test 5: Rendered success and failure output never contains raw `accessToken` or `refreshToken` values from fixture data.
  </behavior>
  <acceptance_criteria>
- `standalone-cli/tests/credential-discovery.test.ts` exists as a standalone `tsx` test surface using fixtures and injected source adapters instead of live local credentials.
- Fixture files exist for a valid Keychain payload and a valid credentials-file payload, while malformed and missing-refresh-token cases are encoded directly in the test suite.
- The test suite covers source-order assertions, typed parse failures, actionable no-credential messaging, and secret-safe output assertions that reject raw token leakage.
- No test reads the real `$HOME/.claude/.credentials.json` path or invokes the real `security` command.
  </acceptance_criteria>
  <action>Write `standalone-cli/tests/credential-discovery.test.ts` first, before implementing discovery logic. Use Node `assert` and the same direct `tsx` pattern as the repo’s root TS tests. Add fixture files under `standalone-cli/tests/fixtures/credential-discovery/` for a valid Keychain-style payload and a valid credentials-file payload, then encode malformed JSON and missing-`claudeAiOauth.refreshToken` cases inline in the test suite so Plan 02 stays below the file-count warning threshold without weakening ENV-01 coverage. Structure the tests so they inject or stub the Keychain and file readers, assert the exact `darwin` ordering rule, and verify the actionable failure guidance from the legacy scripts without depending on a live Keychain entry or a real `~/.claude/.credentials.json` file. Add an explicit assertion that rendered success/failure output never echoes raw `accessToken` or `refreshToken` fixture values. This task only authors the red-side test contract and fixtures; Task 2 is responsible for executing the suite to green after implementation.</action>
  <verify>
    <automated>test -f standalone-cli/tests/credential-discovery.test.ts &amp;&amp; test -f standalone-cli/tests/fixtures/credential-discovery/keychain-valid.json &amp;&amp; test -f standalone-cli/tests/fixtures/credential-discovery/file-valid.json &amp;&amp; rg -n 'darwin|Keychain|credentials file|refreshToken|malformed JSON|Run `claude`|browser login|accessToken|secret-safe|mask' standalone-cli/tests/credential-discovery.test.ts &amp;&amp; ! rg -n 'os\\.homedir\\(|security find-generic-password' standalone-cli/tests/credential-discovery.test.ts</automated>
  </verify>
  <done>The red-side test surface and two reusable fixtures exist first, encode the required source order, parse distinctions, actionable failure contract, and secret-safe output expectations, and can hand off directly to Task 2 for implementation and green execution without depending on live local credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed discovery modules and wire the CLI command surface</name>
  <files>standalone-cli/src/credential-discovery/discover.ts, standalone-cli/src/credential-discovery/parse.ts, standalone-cli/src/credential-discovery/sources/keychain.ts, standalone-cli/src/credential-discovery/sources/credentials-file.ts, standalone-cli/src/cli.ts, standalone-cli/src/output.ts</files>
  <read_first>
.planning/REQUIREMENTS.md
.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-RESEARCH.md
.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md
scripts/quick-setup.sh
scripts/admin-setup.sh
scripts/extract-token.sh
standalone-cli/src/credential-discovery/types.ts
standalone-cli/tests/credential-discovery.test.ts
standalone-cli/src/cli.ts
standalone-cli/src/output.ts
  </read_first>
  <acceptance_criteria>
- `standalone-cli/src/credential-discovery/discover.ts`, `parse.ts`, `sources/keychain.ts`, and `sources/credentials-file.ts` all exist.
- The implemented discovery order is Keychain first on macOS, then credentials file, with non-macOS platforms skipping the Keychain source.
- `parse.ts` validates `claudeAiOauth.refreshToken` and distinguishes malformed JSON, missing refresh token, and source-not-found failures.
- `standalone-cli/src/cli.ts` exposes a credential-discovery command, and `standalone-cli/src/output.ts` renders source-specific success or actionable failure text without printing raw tokens.
- `standalone-cli/tests/credential-discovery.test.ts` passes with an explicit assertion that rendered output masks or omits raw `accessToken` and `refreshToken` fixture values.
- `standalone-cli/tests/cli-help.test.ts` still passes after the `discover-credentials` command wiring so the ISO-02 additive help contract cannot silently regress in Wave 2.
- The package source does not import from `src/`, `scripts/`, `crates/`, or `dist/standalone-cli/`, and the automated tests pass using only fixtures and stubs.
  </acceptance_criteria>
  <action>Implement `standalone-cli/src/credential-discovery/discover.ts`, `parse.ts`, `sources/keychain.ts`, and `sources/credentials-file.ts` against the types created in Plan 01. Begin by using the Task 1 test surface as the contract to satisfy, then make the suite pass in green state here. In `sources/keychain.ts`, wrap `/usr/bin/security find-generic-password -a "$USER" -s "Claude Code-credentials" -w` behind a thin adapter that returns typed success or failure values instead of raw shell output; in `sources/credentials-file.ts`, read and parse a provided or default `~/.claude/.credentials.json` path without mutating it. In `parse.ts`, use `JSON.parse` plus explicit shape checks for `claudeAiOauth.refreshToken`, `accessToken`, `expiresAt`, and optional email fields. In `discover.ts`, preserve the exact ordered fallback from the legacy scripts and collect source failures so the CLI can report actionable guidance when nothing usable is found. Update only the minimum command-surface files in `standalone-cli/src/cli.ts` and `standalone-cli/src/output.ts` to add a `discover-credentials` command and render secret-safe results that satisfy the explicit no-raw-token assertions added in Task 1, while keeping the additive help surface from Plan 01 intact. Do not expand this plan into README/help refresh work, and do not add proxy startup, config generation, runtime preparation, or `claude` launch logic from later phases.</action>
  <verify>
    <automated>test -f standalone-cli/src/credential-discovery/discover.ts &amp;&amp; test -f standalone-cli/src/credential-discovery/parse.ts &amp;&amp; test -f standalone-cli/src/credential-discovery/sources/keychain.ts &amp;&amp; test -f standalone-cli/src/credential-discovery/sources/credentials-file.ts &amp;&amp; rg -n 'discover-credentials|Run `claude`|browser login|Claude Code-credentials|claudeAiOauth|refreshToken' standalone-cli/src/credential-discovery standalone-cli/src/cli.ts standalone-cli/src/output.ts &amp;&amp; rg -n 'accessToken|refreshToken|mask|omit|secret-safe' standalone-cli/tests/credential-discovery.test.ts &amp;&amp; ! rg -n '\.\./oauth\.js|\.\./proxy\.js|dist/standalone-cli|from .*\.\./src/|from .*\.\./scripts/|from .*\.\./crates/' standalone-cli/src standalone-cli/tests &amp;&amp; npm --prefix standalone-cli run build &amp;&amp; npx tsx standalone-cli/tests/credential-discovery.test.ts &amp;&amp; npx tsx standalone-cli/tests/cli-help.test.ts</automated>
  </verify>
  <done>The standalone CLI can run deterministic, typed credential discovery with fixture-driven verification and secret-safe command output, and Wave 2 preserves the additive help contract from Plan 01 while staying inside the isolated package boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine credential stores -> standalone CLI | Sensitive OAuth material crosses from Keychain or a local credentials file into the CLI process. |
| standalone CLI -> operator output | Discovery results must remain actionable without leaking raw access or refresh tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | I | `standalone-cli/src/output.ts` and CLI rendering | mitigate | Render source names, status, and guidance only; never print raw `accessToken` or `refreshToken` values in success or failure output. |
| T-06-02-02 | T | `standalone-cli/src/credential-discovery/discover.ts` | mitigate | Preserve the exact ordered fallback from the legacy scripts and cover it with fixture-driven tests so later edits cannot silently reorder sources. |
| T-06-02-03 | D | Keychain and credentials-file adapters | mitigate | Distinguish “not found”, malformed JSON, and missing `refreshToken` failures so operators get actionable guidance instead of ambiguous exits. |
| T-06-02-04 | E | package implementation boundary | mitigate | Keep the implementation inside `standalone-cli/` and forbid imports from protected legacy paths or stale `dist/standalone-cli/` artifacts. |
</threat_model>

<verification>
- `npm --prefix standalone-cli run build`
- `npx tsx standalone-cli/tests/credential-discovery.test.ts`
- `npx tsx standalone-cli/tests/cli-help.test.ts`
- `! rg -n '\.\./oauth\.js|\.\./proxy\.js|dist/standalone-cli|from .*\.\./src/|from .*\.\./scripts/|from .*\.\./crates/' standalone-cli/src standalone-cli/tests`
</verification>

<success_criteria>
- The isolated standalone CLI package can check supported credential sources in deterministic order and report the source or actionable failure.
- Typed parse errors distinguish malformed content, missing `refreshToken`, and source absence without relying on live Claude credentials for automated verification.
- The command surface remains secret-safe and limited to Phase 6 credential discovery scope, while additive README/help ownership stays in Plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/06-standalone-cli-scaffold-credential-discovery/06-02-SUMMARY.md`
</output>
