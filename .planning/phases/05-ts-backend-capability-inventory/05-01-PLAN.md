---
phase: 05-ts-backend-capability-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md
autonomous: true
requirements:
  - ANA-01
  - ANA-02
must_haves:
  truths:
    - Operator can inspect a checked-in capability matrix covering runtime bootstrap, config loading, client auth, OAuth lifecycle, proxy env, health and verify endpoints, credential extraction, config generation, launcher generation, and claude handoff behavior.
    - Every analyzed capability is classified as must-port, reference-only, or deferred with downstream phase rationale.
    - Known doc and code drift that affects later standalone CLI planning is explicit and source-cited.
  artifacts:
    - .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md
  key_links:
    - Inventory rows cite source files in src/, scripts/, tests/, config.example.yaml, package.json, README.md, or docs/rust-ts-parity-checklist.md.
    - Classification rationale points to Phase 6, Phase 7, Phase 8, or Phase 9 consumers from ROADMAP.md.
---

<objective>
Create the canonical Phase 05 capability inventory as a source-cited matrix that later standalone CLI phases can plan against without re-reading the whole legacy TS surface.

Purpose: Satisfy ANA-01 and ANA-02 with a checked-in artifact that captures the real TypeScript backend surface, not a proxy-only summary or README paraphrase.
Output: `.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md`
</objective>

<execution_context>
@/Users/kay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ts-backend-capability-inventory/05-RESEARCH.md
@.planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md
@package.json
@README.md
@docs/rust-ts-parity-checklist.md
@config.example.yaml
@src/index.ts
@src/config.ts
@src/auth.ts
@src/oauth.ts
@src/proxy.ts
@src/proxy-agent.ts
@src/rewriter.ts
@src/scripts/generate-token.ts
@src/scripts/generate-identity.ts
@scripts/add-client.sh
@scripts/admin-setup.sh
@scripts/quick-setup.sh
@scripts/extract-token.sh
@tests/config.test.ts
@tests/oauth.test.ts
@tests/rewriter.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 05 capability matrix skeleton</name>
  <files>.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/05-ts-backend-capability-inventory/05-RESEARCH.md
package.json
src/index.ts
src/config.ts
src/auth.ts
src/oauth.ts
src/proxy.ts
src/proxy-agent.ts
src/rewriter.ts
src/scripts/generate-token.ts
src/scripts/generate-identity.ts
scripts/add-client.sh
scripts/admin-setup.sh
scripts/quick-setup.sh
scripts/extract-token.sh
config.example.yaml
tests/config.test.ts
tests/oauth.test.ts
tests/rewriter.test.ts
  </read_first>
  <acceptance_criteria>
- `05-CAPABILITY-INVENTORY.md` exists under `.planning/phases/05-ts-backend-capability-inventory/`.
- The file contains a `## Classification Rules` section and a `## Capability Matrix` section.
- The matrix uses the exact columns `Category | Capability | Current TS behavior | Source files | Classification | Downstream phases | Notes`.
- The matrix contains rows covering `runtime bootstrap`, `config loading`, `client auth`, `OAuth lifecycle`, `proxy env`, `health/verify`, `credential extraction`, `config generation`, `launcher generation`, and `claude handoff`.
  </acceptance_criteria>
  <action>Write `.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md` as the canonical checked-in inventory artifact. Add a short phase-purpose section, a `## Classification Rules` section that restricts values to `must-port`, `reference-only`, or `deferred`, and a `## Capability Matrix` section with the exact column headers above. Use repo-relative path citations in the `Source files` column. Keep the artifact documentation-only: do not edit product code, do not leave placeholder `TBD` rows, and do not copy secret, token, or credential values from examples or scripts.</action>
  <verify>
    <automated>test -f .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md &amp;&amp; rg -n "^## Classification Rules$|^## Capability Matrix$|^\| Category \| Capability \| Current TS behavior \| Source files \| Classification \| Downstream phases \| Notes \|$" .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md &amp;&amp; rg -n "runtime bootstrap|config loading|client auth|OAuth lifecycle|proxy env|health/verify|credential extraction|config generation|launcher generation|claude handoff" .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md</automated>
  </verify>
  <done>Matrix skeleton exists with the mandated sections, exact columns, and all planning-critical capability categories present.</done>
</task>

<task type="auto">
  <name>Task 2: Populate classifications, downstream mapping, and drift notes</name>
  <files>.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/05-ts-backend-capability-inventory/05-RESEARCH.md
README.md
docs/rust-ts-parity-checklist.md
src/oauth.ts
src/rewriter.ts
package.json
tests/config.test.ts
tests/oauth.test.ts
tests/rewriter.test.ts
scripts/add-client.sh
scripts/admin-setup.sh
scripts/quick-setup.sh
scripts/extract-token.sh
  </read_first>
  <acceptance_criteria>
- The matrix contains the exact classification values `must-port`, `reference-only`, and `deferred` as row values, not only in prose.
- The file contains a `## Known Drift` section that explicitly names OAuth refresh timing drift, CCH or billing-header narrative drift, and the fact that `tests/config.test.ts` is outside `npm test`.
- Capability notes or downstream columns reference Phase 6, Phase 7, Phase 8, or Phase 9 wherever the inventory informs later work.
- Every drift statement is tied to source files or tests already cited in the document.
  </acceptance_criteria>
  <action>Finish the matrix by classifying every row with exactly one of `must-port`, `reference-only`, or `deferred`, and add downstream rationale tied to Phase 6 through Phase 9 requirements. Add a `## Known Drift` section that records the required doc/code mismatches: README vs `src/oauth.ts`/`tests/oauth.test.ts` refresh timing, README vs `src/rewriter.ts` CCH and billing-header narrative drift, and `package.json` omitting `tests/config.test.ts` from `npm test`. Use source citations from the existing repo files and keep the artifact as an auditable reference document rather than a fix list.</action>
  <verify>
    <automated>rg -n "\| (must-port|reference-only|deferred) \|" .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md &amp;&amp; rg -n "^## Known Drift$|OAuth refresh timing|CCH|billing-header|tests/config.test.ts" .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md &amp;&amp; rg -n "Phase 6|Phase 7|Phase 8|Phase 9" .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md</automated>
  </verify>
  <done>The inventory is source-cited, classified, and explicit about the drift items that later standalone CLI phases must treat as live-repo truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner -> inventory artifact | Human-readable planning output can accidentally overstate or misstate legacy behavior if it is not tied to source files. |
| source files -> checked-in docs | Sensitive bootstrap and credential sources are being summarized; secrets must not leak into the inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | I | `05-CAPABILITY-INVENTORY.md` | mitigate | Cite file paths and behavior summaries only; do not copy raw tokens, OAuth payloads, client secrets, or local credential values from examples or scripts. |
| T-05-01-02 | T | Capability classification rows | mitigate | Require every row to cite source files and tie classification rationale to Phase 6-9 consumers so later plans cannot silently reinterpret behavior. |
| T-05-01-03 | R | Known drift section | mitigate | Record README-vs-code drift explicitly with file references so future work can prove why code/tests were treated as the source of truth. |
</threat_model>

<verification>
- `test -f .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md`
- `rg -n "^## Classification Rules$|^## Capability Matrix$|^## Known Drift$" .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md`
- `rg -n "\| (must-port|reference-only|deferred) \|" .planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md`
- `npm test`
- `npx tsx tests/config.test.ts`
- `npm run build`
</verification>

<success_criteria>
- A checked-in capability inventory exists in the Phase 05 directory.
- The inventory covers the full planning-critical TS backend surface, not just proxy and rewrite behavior.
- Classification values are explicit and reusable by later standalone CLI phases.
- Required drift items are recorded without changing legacy TS, Rust, or script codepaths.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ts-backend-capability-inventory/05-01-SUMMARY.md`
</output>
