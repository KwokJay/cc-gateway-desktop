---
phase: 05-ts-backend-capability-inventory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md
  - .planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md
autonomous: true
requirements:
  - ISO-01
must_haves:
  truths:
    - The milestone has an explicit checked-in boundary stating legacy TypeScript and Rust product paths remain unchanged during standalone CLI work.
    - Phase 05 validation records the required regression commands and a changed-file scope review that can prove protected paths stayed untouched.
    - Phase 05 validation no longer depends on repo-root helper scripts outside the approved planning and documentation scope.
  artifacts:
    - .planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md
    - .planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md
  key_links:
    - Protected-path language names `src/`, `scripts/`, `crates/core/`, `crates/daemon/`, `crates/cli/`, and `crates/desktop/`.
    - Validation includes `npm test`, `npx tsx tests/config.test.ts`, `npm run build`, and a changed-file scope review with expected no matches in protected paths.
---

<objective>
Lock the isolation boundary for the standalone CLI milestone and convert Phase 05 validation into a doc-only guardrail that proves ISO-01 without adding product-code or repo-root script changes.

Purpose: Prevent Phase 05 from being interpreted as permission to modify legacy TS or Rust paths and make the scope restriction mechanically reviewable.
Output: `.planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md` and an updated `.planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md`
</objective>

<execution_context>
@/Users/kay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ts-backend-capability-inventory/05-RESEARCH.md
@.planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md
@AGENTS.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the explicit Phase 05 isolation boundary contract</name>
  <files>.planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md</files>
  <read_first>
.planning/STATE.md
.planning/PROJECT.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/05-ts-backend-capability-inventory/05-RESEARCH.md
AGENTS.md
  </read_first>
  <acceptance_criteria>
- `05-ISOLATION-BOUNDARY.md` exists in the Phase 05 directory.
- The file contains a `## Protected Legacy Paths` section listing `src/`, `scripts/`, `crates/core/`, `crates/daemon/`, `crates/cli/`, and `crates/desktop/`.
- The file explicitly states that these legacy paths remain unchanged during the standalone CLI milestone and that Phase 05 is documentation-and-boundary work only.
- The file names the allowed Phase 05 output area under `.planning/phases/05-ts-backend-capability-inventory/`.
  </acceptance_criteria>
  <action>Create `.planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md` as the canonical ISO-01 boundary artifact. Include sections for `## Boundary Statement`, `## Protected Legacy Paths`, `## Allowed Phase 05 Output Paths`, and `## Implications For Later Phases`. State plainly that legacy TypeScript gateway files, setup scripts, Rust daemon, Rust CLI, and desktop paths remain unchanged while the new standalone CLI is developed in an isolated future path or package. Keep the document normative and milestone-specific; do not propose edits to protected paths.</action>
  <verify>
    <automated>test -f .planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md &amp;&amp; rg -n "^## Protected Legacy Paths$|src/|scripts/|crates/core/|crates/daemon/|crates/cli/|crates/desktop/" .planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md &amp;&amp; rg -n "remain unchanged|documentation-and-boundary|isolated future path|package" .planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md</automated>
  </verify>
  <done>A checked-in boundary artifact exists and explicitly protects the legacy TS and Rust paths for the standalone CLI milestone.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Phase 05 validation to enforce scope and regression checks without repo-root scripts</name>
  <files>.planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/05-ts-backend-capability-inventory/05-RESEARCH.md
.planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md
.planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md
package.json
  </read_first>
  <acceptance_criteria>
- `05-VALIDATION.md` frontmatter sets `nyquist_compliant: true` and `wave_0_complete: true`.
- The validation document includes `npm test`, `npx tsx tests/config.test.ts`, `npm run build`, and a changed-file scope review command that flags changes under protected legacy paths.
- The validation document does not reference `scripts/check-phase-05-inventory.mjs` or `scripts/check-phase-05-scope.sh`.
- The changed-file scope review is documented as expecting no matches for `src/`, `scripts/`, `crates/core/`, `crates/daemon/`, `crates/cli/`, or `crates/desktop/`.
  </acceptance_criteria>
  <action>Update `.planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md` so it is fully phase-compliant and doc-only. Remove the Wave 0 dependency on repo-root helper scripts, mark the phase as Nyquist-compliant, and keep validation inside the phase directory. Record the required automated guards exactly: `npm test`, `npx tsx tests/config.test.ts`, `npm run build`, and a changed-file scope review command such as `git diff --name-only -- . | rg -n '^(src/|scripts/|crates/core/|crates/daemon/|crates/cli/|crates/desktop/)'` with the expectation of no output. Tie the scope review back to `05-ISOLATION-BOUNDARY.md` so ISO-01 is provable from checked-in docs alone.</action>
  <verify>
    <automated>rg -n "nyquist_compliant: true|wave_0_complete: true" .planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md &amp;&amp; rg -n "npm test|npx tsx tests/config.test.ts|npm run build|git diff --name-only -- . \| rg -n '\\^\\(src/\\|scripts/\\|crates/core/\\|crates/daemon/\\|crates/cli/\\|crates/desktop/\\)'" .planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md &amp;&amp; ! rg -n "scripts/check-phase-05-inventory.mjs|scripts/check-phase-05-scope.sh" .planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md</automated>
  </verify>
  <done>The validation contract enforces the required regression and scope checks without asking for repo-root scripts or product-code edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner -> boundary contract | Ambiguous wording can be misread as permission to modify protected legacy paths. |
| validation contract -> execution behavior | Weak or incomplete validation instructions can let Phase 05 claim ISO-01 without actually checking scope drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T | `05-ISOLATION-BOUNDARY.md` | mitigate | Enumerate protected legacy paths explicitly and state that standalone CLI work must remain isolated from them during this milestone. |
| T-05-02-02 | R | `05-VALIDATION.md` | mitigate | Record concrete regression commands and a changed-file scope review so ISO-01 can be demonstrated from CLI output instead of narrative claims. |
| T-05-02-03 | E | Phase 05 validation workflow | mitigate | Remove repo-root helper-script requirements so this documentation phase cannot expand itself into unauthorized implementation work outside the approved planning directory. |
</threat_model>

<verification>
- `test -f .planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md`
- `rg -n "^## Protected Legacy Paths$|src/|scripts/|crates/core/|crates/daemon/|crates/cli/|crates/desktop/" .planning/phases/05-ts-backend-capability-inventory/05-ISOLATION-BOUNDARY.md`
- `rg -n "nyquist_compliant: true|wave_0_complete: true|npm test|npx tsx tests/config.test.ts|npm run build" .planning/phases/05-ts-backend-capability-inventory/05-VALIDATION.md`
- `npm test`
- `npx tsx tests/config.test.ts`
- `npm run build`
- `git diff --name-only -- . | rg -n '^(src/|scripts/|crates/core/|crates/daemon/|crates/cli/|crates/desktop/)'`
  Expected result: no output.
</verification>

<success_criteria>
- A checked-in boundary artifact states that legacy TS and Rust paths remain unchanged during the standalone CLI milestone.
- The validation contract proves ISO-01 with CLI-reviewable commands and no repo-root helper scripts.
- Protected legacy paths are named explicitly, and the changed-file scope review is part of the required verification surface.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ts-backend-capability-inventory/05-02-SUMMARY.md`
</output>
