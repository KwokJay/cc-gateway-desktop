---
phase: 09-validation-operator-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - standalone-cli/package.json
  - standalone-cli/tests/run-all.ts
  - standalone-cli/tests/capability-inventory.test.ts
autonomous: true
requirements:
  - QLT-01
must_haves:
  truths:
    - Reviewers can run one package-local command to execute the standalone CLI validation surface without live OAuth or a real `claude` subprocess.
    - The Phase 05 capability inventory stays enforced as an executable contract instead of drifting as unchecked planning prose.
    - The aggregate validation surface still proves credential discovery, bootstrap generation, runtime preparation, and launch handoff using only checked-in fixtures and injected doubles.
  artifacts:
    - standalone-cli/package.json
    - standalone-cli/tests/run-all.ts
    - standalone-cli/tests/capability-inventory.test.ts
  key_links:
    - `standalone-cli/package.json` exposes `npm test` as the aggregate package-local validation command for Phase 9.
    - `standalone-cli/tests/run-all.ts` deterministically executes every package-local `*.test.ts` contract without touching helpers, fixtures, or live operator state.
    - `standalone-cli/tests/capability-inventory.test.ts` reads `.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md` and enforces its classification and drift contract.
---

<objective>
Add the missing durable proof surface for Phase 9 by creating an aggregate standalone validation command and a package-local contract test for the Phase 05 capability inventory.

Purpose: Satisfy `QLT-01` without adding new runtime behavior, while keeping validation package-local, fixture-driven, and aligned to the isolated `standalone-cli/` boundary.
Output: A deterministic package-local test runner, a capability-inventory contract test, and an aggregate `npm test` command for standalone reviewers.
</objective>

<execution_context>
@/Users/kay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-validation-operator-guidance/09-RESEARCH.md
@.planning/phases/09-validation-operator-guidance/09-VALIDATION.md
@.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md
@.planning/phases/05-ts-backend-capability-inventory/05-01-SUMMARY.md
@.planning/phases/06-standalone-cli-scaffold-credential-discovery/VERIFICATION.md
@.planning/phases/07-local-environment-construction-runtime-preparation/VERIFICATION.md
@.planning/phases/08-claude-launch-handoff/VERIFICATION.md
@AGENTS.md
@standalone-cli/package.json
@standalone-cli/tests/cli-help.test.ts
@standalone-cli/tests/credential-discovery.test.ts
@standalone-cli/tests/environment-bootstrap.test.ts
@standalone-cli/tests/proxy-env.test.ts
@standalone-cli/tests/runtime-preparation.test.ts
@standalone-cli/tests/claude-launch.test.ts

<interfaces>
From `standalone-cli/package.json`, the current package has targeted scripts but no aggregate reviewer command:

```json
{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test:environment": "tsx tests/environment-bootstrap.test.ts",
    "test:proxy-env": "tsx tests/proxy-env.test.ts",
    "test:runtime": "tsx tests/runtime-preparation.test.ts",
    "test:launch": "tsx tests/claude-launch.test.ts",
    "test:help": "tsx tests/cli-help.test.ts",
    "test:credentials": "tsx tests/credential-discovery.test.ts"
  }
}
```

From `.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md`, the enforced classification vocabulary is fixed:

```text
must-port
reference-only
deferred
```

From the existing standalone package surface, Phase 9 must keep validation package-local and live-OAuth-free by covering these checks:

```text
cli-help.test.ts
credential-discovery.test.ts
environment-bootstrap.test.ts
proxy-env.test.ts
runtime-preparation.test.ts
claude-launch.test.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the aggregate standalone test runner and capability-inventory contract</name>
  <files>standalone-cli/tests/run-all.ts, standalone-cli/tests/capability-inventory.test.ts</files>
  <read_first>
.planning/REQUIREMENTS.md
.planning/phases/09-validation-operator-guidance/09-RESEARCH.md
.planning/phases/09-validation-operator-guidance/09-VALIDATION.md
.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md
.planning/phases/05-ts-backend-capability-inventory/05-01-SUMMARY.md
.planning/phases/06-standalone-cli-scaffold-credential-discovery/VERIFICATION.md
.planning/phases/07-local-environment-construction-runtime-preparation/VERIFICATION.md
.planning/phases/08-claude-launch-handoff/VERIFICATION.md
AGENTS.md
standalone-cli/package.json
standalone-cli/tests/cli-help.test.ts
standalone-cli/tests/credential-discovery.test.ts
standalone-cli/tests/environment-bootstrap.test.ts
standalone-cli/tests/proxy-env.test.ts
standalone-cli/tests/runtime-preparation.test.ts
standalone-cli/tests/claude-launch.test.ts
  </read_first>
  <behavior>
    - Test 1: The aggregate runner discovers package-local `tests/*.test.ts` files deterministically, excludes helper and fixture paths, and fails fast when any contract fails.
    - Test 2: The aggregate runner executes the existing standalone validation surface without requiring live OAuth, a real `claude` binary, or a real operator workspace.
    - Test 3: The capability-inventory contract test enforces required rows, allowed classifications, downstream Phase 9 coverage, and the presence of the `Known Drift` section in the Phase 05 artifact.
    - Test 4: The contract test rejects stale or incomplete inventory content such as missing `claude handoff`, `credential extraction`, or `config generation` rows.
  </behavior>
  <acceptance_criteria>
- `standalone-cli/tests/run-all.ts` exists and executes package-local `*.test.ts` files in sorted order while excluding `tests/helpers/**` and `tests/fixtures/**`.
- `standalone-cli/tests/run-all.ts` is generic enough that a later `standalone-cli/tests/operator-guidance.test.ts` file will be picked up automatically without another package.json change.
- `standalone-cli/tests/capability-inventory.test.ts` exists and reads `.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md`.
- `standalone-cli/tests/capability-inventory.test.ts` grep-verifiably asserts allowed classifications (`must-port`, `reference-only`, `deferred`), the `Known Drift` heading, and current critical capability rows including `config loading`, `credential extraction`, `config generation`, and `claude handoff`.
- Both new test files stay fixture-driven and checked-in-data-driven only; they must not invoke network access, read live credentials, or require a real `claude` subprocess.
  </acceptance_criteria>
  <action>Create `standalone-cli/tests/run-all.ts` as the package-local Phase 9 test harness. It should use Node built-ins plus `child_process` to enumerate `standalone-cli/tests/*.test.ts`, skip helper or fixture files, sort the list deterministically, and spawn `tsx` once per test file so failures are isolated and easy to attribute. Keep the runner generic so new package-local tests are included automatically. Then add `standalone-cli/tests/capability-inventory.test.ts` as a string-contract test against `.planning/phases/05-ts-backend-capability-inventory/05-CAPABILITY-INVENTORY.md`: assert the document still contains the capability matrix, the allowed classification vocabulary, the Phase 9 downstream mapping, and the `Known Drift` section, plus critical rows for `config loading`, `credential extraction`, `config generation`, and `claude handoff`. Use Node `assert` and file reads only; do not import legacy runtime modules, do not shell out to live gateway scripts, and do not depend on operator credentials.</action>
  <verify>
    <automated>test -f standalone-cli/tests/run-all.ts &amp;&amp; test -f standalone-cli/tests/capability-inventory.test.ts &amp;&amp; rg -n 'tests/.*\\.test\\.ts|helpers|fixtures|tsx' standalone-cli/tests/run-all.ts &amp;&amp; rg -n '05-CAPABILITY-INVENTORY\\.md|must-port|reference-only|deferred|Known Drift|config loading|credential extraction|config generation|claude handoff' standalone-cli/tests/capability-inventory.test.ts</automated>
  </verify>
  <done>The standalone package has a deterministic aggregate test harness and an executable contract for the Phase 05 capability inventory, ready for `npm test` wiring in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Wire package-local `npm test` to the aggregate validation surface</name>
  <files>standalone-cli/package.json</files>
  <read_first>
.planning/REQUIREMENTS.md
.planning/phases/09-validation-operator-guidance/09-RESEARCH.md
.planning/phases/09-validation-operator-guidance/09-VALIDATION.md
AGENTS.md
standalone-cli/package.json
standalone-cli/tests/run-all.ts
standalone-cli/tests/capability-inventory.test.ts
standalone-cli/tests/cli-help.test.ts
standalone-cli/tests/credential-discovery.test.ts
standalone-cli/tests/environment-bootstrap.test.ts
standalone-cli/tests/proxy-env.test.ts
standalone-cli/tests/runtime-preparation.test.ts
standalone-cli/tests/claude-launch.test.ts
  </read_first>
  <acceptance_criteria>
- `standalone-cli/package.json` defines a top-level `test` script so `npm --prefix standalone-cli test` succeeds instead of returning `Missing script: "test"`.
- The `test` script runs the package-local aggregate validation surface from Task 1 and keeps the existing `build` contract in the flow.
- `npm --prefix standalone-cli test` executes the current standalone tests and the new capability-inventory contract without live OAuth traffic.
- The aggregate validation path remains package-local and does not invoke protected legacy runtime entrypoints under `src/`, `scripts/`, or `crates/`.
  </acceptance_criteria>
  <action>Update `standalone-cli/package.json` to expose the canonical reviewer command for Phase 9: `npm test`. Make that script run the package-local build plus the new `standalone-cli/tests/run-all.ts` harness from Task 1 so reviewers no longer need to remember individual `test:*` commands. Preserve the existing targeted `test:*` scripts so focused reruns remain available during execution, but make `npm --prefix standalone-cli test` the durable aggregate proof surface referenced by later docs. Do not add dependencies, do not move tests outside `standalone-cli/tests/`, and do not invoke legacy repo-root runtime scripts.</action>
  <verify>
    <automated>rg -n '"test"\\s*:' standalone-cli/package.json &amp;&amp; npm --prefix standalone-cli test</automated>
  </verify>
  <done>`npm --prefix standalone-cli test` becomes the single package-local validation command for the standalone CLI and proves the current standalone behavior plus the Phase 05 inventory contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregate test command -> local execution surface | The Phase 9 runner launches checked-in tests and must not silently skip required validation or reach into live machine state. |
| planning artifact -> contract test | A checked-in planning document is treated as an executable contract and must not drift without failing validation. |
| test output -> operator environment | Validation logs must stay secret-safe and avoid reading or printing raw credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | T | `standalone-cli/tests/run-all.ts` test enumeration | mitigate | Enumerate `tests/*.test.ts` deterministically, exclude only helpers/fixtures, and fail on the first non-zero child exit so no required contract is skipped silently. |
| T-09-01-02 | I | aggregate validation surface | mitigate | Keep the runner limited to checked-in package-local tests that already use fixtures and dependency injection; do not read live OAuth files or launch a real `claude` subprocess. |
| T-09-01-03 | T | `standalone-cli/tests/capability-inventory.test.ts` | mitigate | Assert required headings, row names, and allowed classification values directly against `05-CAPABILITY-INVENTORY.md` so docs drift breaks validation immediately. |
| T-09-01-04 | E | validation scope | mitigate | Keep all Phase 9 code inside `standalone-cli/` and `.planning/`; the aggregate command must not shell into protected legacy runtime paths or modify them. |
</threat_model>

<verification>
- `npm --prefix standalone-cli test`
- `npx tsx standalone-cli/tests/capability-inventory.test.ts`
</verification>

<success_criteria>
- Reviewers can run `npm --prefix standalone-cli test` to execute the standalone validation surface in one step.
- The aggregate test path remains live-OAuth-free and package-local.
- The Phase 05 capability inventory is protected by an executable contract instead of manual review alone.
</success_criteria>

<output>
After completion, create `.planning/phases/09-validation-operator-guidance/09-01-SUMMARY.md`
</output>
