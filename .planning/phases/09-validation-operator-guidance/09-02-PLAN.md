---
phase: 09-validation-operator-guidance
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - standalone-cli/README.md
  - docs/standalone-cli.md
  - standalone-cli/tests/operator-guidance.test.ts
autonomous: true
requirements:
  - QLT-02
must_haves:
  truths:
    - Operators can follow one canonical checked-in guide for first run, repeat run, missing credentials, and missing `claude` recovery.
    - The repo-level standalone CLI doc no longer competes with the package README as a second authoritative handbook.
    - Documentation stays aligned to the live standalone package paths, commands, and failure guidance instead of Phase 0-era placeholders.
  artifacts:
    - standalone-cli/README.md
    - docs/standalone-cli.md
    - standalone-cli/tests/operator-guidance.test.ts
  key_links:
    - `standalone-cli/tests/operator-guidance.test.ts` enforces the README and repo doc against the current command and path surface.
    - `standalone-cli/README.md` becomes the canonical operator guide and references the aggregate `npm --prefix standalone-cli test` command from Plan 09-01.
    - `docs/standalone-cli.md` becomes a thin overview or pointer that sends readers to `standalone-cli/README.md` instead of duplicating full procedures.
---

<objective>
Collapse standalone operator guidance to one canonical README, reduce the stale repo doc to an overview or pointer, and add a contract test that enforces the required operator scenarios.

Purpose: Satisfy `QLT-02` while reducing documentation drift and keeping the final guidance surface accurate, secret-safe, and aligned to the actual standalone package behavior.
Output: A canonical operator runbook in `standalone-cli/README.md`, a synced overview doc in `docs/standalone-cli.md`, and a docs contract test that keeps them aligned.
</objective>

<execution_context>
@/Users/kay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-validation-operator-guidance/09-RESEARCH.md
@.planning/phases/09-validation-operator-guidance/09-VALIDATION.md
@.planning/phases/06-standalone-cli-scaffold-credential-discovery/VERIFICATION.md
@.planning/phases/07-local-environment-construction-runtime-preparation/VERIFICATION.md
@.planning/phases/08-claude-launch-handoff/VERIFICATION.md
@.planning/phases/09-validation-operator-guidance/09-01-PLAN.md
@AGENTS.md
@standalone-cli/package.json
@standalone-cli/README.md
@docs/standalone-cli.md
@standalone-cli/src/output.ts
@standalone-cli/tests/cli-help.test.ts
@standalone-cli/tests/credential-discovery.test.ts
@standalone-cli/tests/environment-bootstrap.test.ts
@standalone-cli/tests/runtime-preparation.test.ts
@standalone-cli/tests/claude-launch.test.ts

<interfaces>
From `standalone-cli/src/output.ts`, the live command surface and failure guidance already exist and the docs must match them:

```text
ccgw-standalone-cli [claude args]
ccgw-standalone-cli discover-credentials
ccgw-standalone-cli prepare-runtime
```

```text
Install Claude Code if needed: npm install -g @anthropic-ai/claude-code
Open a new shell and confirm claude --help works from PATH
Retry ccgw-standalone-cli [claude args]
```

From Phase 7 and Phase 8 verification, the operator-visible workspace artifacts are fixed:

```text
~/.ccgw/standalone-cli/manifest.json
~/.ccgw/standalone-cli/config.yaml
~/.ccgw/standalone-cli/runtime.json
```

From Plan 09-01, the canonical package-local validation command must be documented exactly:

```text
npm --prefix standalone-cli test
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a docs contract test for first run, repeat run, and recovery guidance</name>
  <files>standalone-cli/tests/operator-guidance.test.ts</files>
  <read_first>
.planning/REQUIREMENTS.md
.planning/phases/09-validation-operator-guidance/09-RESEARCH.md
.planning/phases/09-validation-operator-guidance/09-VALIDATION.md
.planning/phases/06-standalone-cli-scaffold-credential-discovery/VERIFICATION.md
.planning/phases/07-local-environment-construction-runtime-preparation/VERIFICATION.md
.planning/phases/08-claude-launch-handoff/VERIFICATION.md
.planning/phases/09-validation-operator-guidance/09-01-PLAN.md
AGENTS.md
standalone-cli/package.json
standalone-cli/README.md
docs/standalone-cli.md
standalone-cli/src/output.ts
standalone-cli/tests/cli-help.test.ts
standalone-cli/tests/credential-discovery.test.ts
standalone-cli/tests/environment-bootstrap.test.ts
standalone-cli/tests/runtime-preparation.test.ts
standalone-cli/tests/claude-launch.test.ts
  </read_first>
  <behavior>
    - Test 1: `standalone-cli/README.md` contains the canonical first-run flow from credential discovery through runtime preparation into direct Claude handoff.
    - Test 2: The README explains repeat-run behavior in terms of reuse or refresh of `manifest.json`, `config.yaml`, and `runtime.json` under `~/.ccgw/standalone-cli/`.
    - Test 3: The README includes actionable recovery guidance for missing credentials and missing `claude`, using the current command names and install/PATH instructions.
    - Test 4: `docs/standalone-cli.md` is reduced to an aligned overview or pointer and does not retain stale terms such as `src/standalone-cli`, `~/.ccg-local-cli`, `setup`, or `status`.
    - Test 5: The docs mention `npm --prefix standalone-cli test` so Phase 09 validation is discoverable from the canonical operator guide.
  </behavior>
  <acceptance_criteria>
- `standalone-cli/tests/operator-guidance.test.ts` exists and reads both `standalone-cli/README.md` and `docs/standalone-cli.md`.
- The contract test grep-verifiably asserts current commands (`discover-credentials`, `prepare-runtime`, bare `ccgw-standalone-cli [claude args]`), current workspace paths (`~/.ccgw/standalone-cli/manifest.json`, `config.yaml`, `runtime.json`), and the aggregate validation command (`npm --prefix standalone-cli test`).
- The contract test fails on stale repo-doc markers including `src/standalone-cli`, `~/.ccg-local-cli`, `setup`, `status`, and “start the TypeScript gateway logic in-process”.
- The contract test keeps docs secret-safe by checking for guidance language and paths only, not raw token examples.
  </acceptance_criteria>
  <action>Create `standalone-cli/tests/operator-guidance.test.ts` as a string-contract test for the Phase 9 docs surface. Read `standalone-cli/README.md` and `docs/standalone-cli.md` directly with Node file APIs and use `assert` to enforce the required operator scenarios: first run, repeat run, missing credentials, and missing `claude`. Require the live command surface from `standalone-cli/src/output.ts`, the workspace artifact paths from Phase 7, and the install/PATH guidance from Phase 8. Also require the canonical validation command `npm --prefix standalone-cli test`. In the repo-level doc assertions, explicitly reject stale Phase 0 terms like `src/standalone-cli`, `~/.ccg-local-cli`, `setup`, `status`, and in-process gateway startup. Keep the contract test broad enough to survive copy edits, but specific enough that docs drift fails immediately.</action>
  <verify>
    <automated>test -f standalone-cli/tests/operator-guidance.test.ts &amp;&amp; rg -n 'standalone-cli/README.md|docs/standalone-cli.md|discover-credentials|prepare-runtime|ccgw-standalone-cli \\[claude args\\]|~/.ccgw/standalone-cli/manifest.json|~/.ccgw/standalone-cli/config.yaml|~/.ccgw/standalone-cli/runtime.json|npm --prefix standalone-cli test|src/standalone-cli|~/.ccg-local-cli|setup|status' standalone-cli/tests/operator-guidance.test.ts</automated>
  </verify>
  <done>The Phase 9 operator-doc scenarios are enforced by a package-local contract test before any doc rewrite happens.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the canonical operator guide and reduce the repo doc to an overview pointer</name>
  <files>standalone-cli/README.md, docs/standalone-cli.md</files>
  <read_first>
.planning/REQUIREMENTS.md
.planning/phases/09-validation-operator-guidance/09-RESEARCH.md
.planning/phases/09-validation-operator-guidance/09-VALIDATION.md
.planning/phases/09-validation-operator-guidance/09-01-PLAN.md
AGENTS.md
standalone-cli/package.json
standalone-cli/README.md
docs/standalone-cli.md
standalone-cli/src/output.ts
standalone-cli/tests/operator-guidance.test.ts
standalone-cli/tests/cli-help.test.ts
standalone-cli/tests/credential-discovery.test.ts
standalone-cli/tests/environment-bootstrap.test.ts
standalone-cli/tests/runtime-preparation.test.ts
standalone-cli/tests/claude-launch.test.ts
  </read_first>
  <acceptance_criteria>
- `standalone-cli/README.md` becomes the canonical operator guide and includes: scope/additive boundary, first-run flow, repeat-run behavior, workspace artifacts, missing-credentials recovery, missing-`claude` recovery, and package-local validation commands.
- `standalone-cli/README.md` uses the live standalone command surface and current paths only: `ccgw-standalone-cli`, `discover-credentials`, `prepare-runtime`, and `~/.ccgw/standalone-cli/{manifest.json,config.yaml,runtime.json}`.
- `docs/standalone-cli.md` is reduced to a short aligned overview or pointer that directs readers to `standalone-cli/README.md` for the authoritative runbook and does not duplicate long-form procedure content.
- The rewritten docs mention `npm --prefix standalone-cli test` and avoid stale or incorrect guidance like `src/standalone-cli`, `~/.ccg-local-cli`, `setup`, `status`, or in-process gateway startup.
- `npx tsx standalone-cli/tests/operator-guidance.test.ts` passes, and `npm --prefix standalone-cli test` also passes with the new docs test included automatically.
  </acceptance_criteria>
  <action>Rewrite `standalone-cli/README.md` into the canonical Phase 9 operator guide. Keep it explicitly additive, then add concrete sections covering: what the standalone CLI does and does not replace; first run (`ccgw-standalone-cli` or `discover-credentials` / `prepare-runtime`), repeat run behavior and reuse of `manifest.json`, `config.yaml`, and `runtime.json` under `~/.ccgw/standalone-cli/`, missing-credentials recovery (run `claude`, complete login, retry), missing-`claude` recovery (`npm install -g @anthropic-ai/claude-code`, refresh PATH, retry), and package-local validation commands with `npm --prefix standalone-cli test` as the primary reviewer entrypoint. Then reduce `docs/standalone-cli.md` to a thin discoverability document: short purpose statement, additive boundary reminder, and a pointer to `standalone-cli/README.md` for the authoritative runbook. Do not preserve stale procedural content from the old repo doc, and do not add a second long-form authoritative guide.</action>
  <verify>
    <automated>npx tsx standalone-cli/tests/operator-guidance.test.ts &amp;&amp; npm --prefix standalone-cli test</automated>
  </verify>
  <done>The standalone CLI has one canonical checked-in operator guide, the repo doc no longer drifts as a second handbook, and the required scenarios stay locked by automated docs validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in docs -> operator actions | Operators will follow the README and repo doc to prepare a real local environment and launch Claude. |
| canonical README -> repo-level overview doc | Two docs surfaces must stay aligned without becoming competing authorities. |
| docs examples -> sensitive bootstrap state | Documentation must describe credential and client-token handling without exposing real secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | T | `standalone-cli/README.md` command and path guidance | mitigate | Enforce current commands, workspace paths, and recovery text with `standalone-cli/tests/operator-guidance.test.ts` so stale instructions fail validation. |
| T-09-02-02 | I | README and repo doc examples | mitigate | Keep docs secret-safe by documenting location and status only; forbid raw access tokens, refresh tokens, or client-token literals in examples and assertions. |
| T-09-02-03 | S | duplicate authoritative docs | mitigate | Reduce `docs/standalone-cli.md` to a thin overview/pointer that explicitly delegates full procedure ownership to `standalone-cli/README.md`. |
| T-09-02-04 | D | recovery guidance for missing credentials or `claude` | mitigate | Require explicit remediation steps in the canonical README and lock them with the docs contract test so operators are not blocked by vague failure text. |
</threat_model>

<verification>
- `npx tsx standalone-cli/tests/operator-guidance.test.ts`
- `npm --prefix standalone-cli test`
</verification>

<success_criteria>
- `standalone-cli/README.md` is the single authoritative operator guide for the standalone CLI.
- The docs explicitly cover first run, repeat run, missing credentials, and missing `claude`.
- `docs/standalone-cli.md` no longer duplicates stale detailed procedures and instead points to the canonical guide.
</success_criteria>

<output>
After completion, create `.planning/phases/09-validation-operator-guidance/09-02-SUMMARY.md`
</output>
